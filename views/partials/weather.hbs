<div class="weatherWidget">
    {{#each partials.weatherContext}}
        <div class="location">
            <h3>{{location.name}}</h3>
            <a href="{{locaion.forecastUrl}}">
                <img src="{{iconUrl}}" alt="{{weather}}">
                {{weather}}, {{temp}}
            </a>
        </div>
    {{/each}}
    <small>출처 : <a href="https://www.weather.gov.documentation/sevices-web-api"></a></a></small>
    국가 날씨 서비스
</div>